name: acadpro_reminder_app
description: A simple productivity reminder app with dark mode, rotating tips, and app launcher links.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  url_launcher: ^6.2.6

flutter:
  uses-material-design: true
